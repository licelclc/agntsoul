# AgntSoul 首页检查与部署清单

> 更新时间：2026年4月14日

---

## ✅ 已完成的修复

| 修复项 | 状态 | 说明 |
|--------|------|------|
| 页面标题修复 | ✅ | 已将 "FerryMind" 改为 "AgntSoul - AI人格市场" |
| 搜索页面创建 | ✅ | 已创建 `/app/search/page.tsx`，支持搜索功能 |
| .env 示例文件 | ✅ | 已创建 `.env.local.example` |

---

## 🔧 部署前检查清单

### 1. 配置环境变量 (必须)

从 Supabase Dashboard 获取以下信息：
- **Project URL**: Settings → API → Project URL
- **Anon Key**: Settings → API → Project API keys → anon public
- **Service Role Key**: Settings → API → Project API keys → service_role (保密!)

创建 `.env.local` 文件：

```bash
cd AI人格平台MVP
cp .env.local.example .env.local
# 编辑 .env.local，填入实际的 Supabase 配置
```

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
```

### 2. 安装依赖

```bash
cd AI人格平台MVP
npm install
```

### 3. 初始化数据库 (如尚未完成)

在 Supabase SQL Editor 中执行 `database/schema.sql`

### 4. 本地测试

```bash
npm run dev
# 访问 http://localhost:3000
```

### 5. 部署到 Vercel

1. 将代码推送到 GitHub
2. 在 Vercel 导入项目
3. 配置环境变量 (在 Vercel Dashboard → Settings → Environment Variables)
4. 部署

---

## 📁 项目结构

```
AI人格平台MVP/
├── app/
│   ├── page.tsx           # 首页 ✅
│   ├── layout.tsx         # 布局 ✅ (已修复标题)
│   ├── globals.css        # 全局样式 ✅
│   ├── search/
│   │   └── page.tsx       # 搜索页 ✅ (新建)
│   ├── upload/
│   │   └── page.tsx       # 上传页 ✅
│   ├── p/[id]/
│   │   └── page.tsx       # 人格详情页 ✅
│   └── api/
│       ├── personalities/ # API ✅
│       └── search/        # 搜索API ✅
├── components/
│   ├── PersonalityCard.tsx ✅
│   ├── SearchBar.tsx      ✅
│   └── UploadButton.tsx   ✅
├── lib/
│   └── supabase.ts        ✅
├── types/
│   └── personality.ts     ✅
├── database/
│   └── schema.sql         ✅
└── .env.local.example     ✅ (新建)
```

---

## 🌐 访问域名

- **生产环境**: https://agntsoul.com
- **本地开发**: http://localhost:3000

---

## ❓ 常见问题

### Q: 首页显示空白?
A: 检查浏览器控制台是否有 Supabase 连接错误，确认 .env.local 配置正确

### Q: 搜索功能不工作?
A: 确认已创建 search 页面 (已修复)

### Q: 上传按钮无效?
A: 检查 /upload 页面是否存在 (存在)

### Q: 如何添加示例人格?
A: 参考 `示例人格/` 目录下的 JSON 文件，在 Supabase Table Editor 中手动添加
